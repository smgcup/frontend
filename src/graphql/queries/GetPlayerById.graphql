query PlayerById($id: String!) {
  playerById(id: $id) {
    id
    firstName
    lastName
    height
    weight
    imageUrl
    preferredFoot
    position
    team {
      id
      name
    }
  }
}
