query TeamById($id: String!) {
  teamById(id: $id) {
    id
    name
    createdAt
    players {
      age
      id
      firstName
      lastName
      position
      imageUrl
    }
    captain {
      id
      firstName
      lastName
      position
      age
      imageUrl
    }
  }
}
