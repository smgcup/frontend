query PredictionLeaderboard {
  predictionLeaderboard {
    id
    totalPoints
    exactMatchesCount
    correctOutcomesCount
    totalPredictionsCount
    lastUpdated
    user {
      id
      username
      firstName
      lastName
      email
    }
  }
}
