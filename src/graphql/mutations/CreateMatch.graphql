mutation CreateMatch($dto: CreateMatchDto!) {
  createMatch(createMatchDto: $dto) {
    id
    date
    status
    score1
    score2
    round
    location
    firstOpponent {
      id
      name
    }
    secondOpponent {
      id
      name
    }
  }
}
