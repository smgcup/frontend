mutation UpdateTeam($id: String!, $dto: UpdateTeamDto!) {
  updateTeam(id: $id, updateTeamDto: $dto) {
    id
    name
    createdAt
    captain {
      id
      firstName
      lastName
      position
    }
  }
}
