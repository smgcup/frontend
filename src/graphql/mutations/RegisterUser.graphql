mutation RegisterUser($registerInput: RegisterUserInput!) {
  register(registerInput: $registerInput) {
    accessToken
    user {
      id
      email
      username
      firstName
      lastName
    }
  }
}
