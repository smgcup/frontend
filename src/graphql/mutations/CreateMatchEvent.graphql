mutation CreateMatchEvent($dto: CreateMatchEventDto!) {
  createMatchEvent(createMatchEventDto: $dto) {
    id
    type
    minute
    createdAt
    team {
      id
      name
    }
    player {
      id
      firstName
      lastName
    }
  }
}
