mutation UpdateMatch($id: String!, $dto: UpdateMatchDto!) {
  updateMatch(id: $id, updateMatchDto: $dto) {
    id
    date
    status
    score1
    score2
    round
    fdr1
    fdr2
    location
    firstOpponent {
      id
      name
    }
    secondOpponent {
      id
      name
    }
  }
}
